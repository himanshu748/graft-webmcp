{
  "name": "graft-webmcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:runtime && vite",
    "build": "npm run build:runtime && tsc -b && vite build",
    "preview": "vite preview",
    "test": "npm run build:runtime && vitest run",
    "test:watch": "npm run build:runtime && vitest",
    "build:runtime": "vite build --config vite.runtime.config.ts",
    "build:owner-adapter": "npm run build:runtime && vite-node scripts/build-owner-adapter.ts",
    "check:owner-adapter": "npm run build:owner-adapter && git diff --exit-code -- examples/owner-site/graft-adapter.js"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "@phosphor-icons/react": "^2.1.10",
    "@sparticuz/chromium": "^149.0.0",
    "puppeteer-core": "^25.9.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.2",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vite-node": "3.2.4",
    "vitest": "^3.2.4"
  }
}
